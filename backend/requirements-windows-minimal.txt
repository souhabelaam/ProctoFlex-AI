# Dépendances minimales pour Windows - Installation de base
# Ces packages sont essentiels pour faire fonctionner l'application

# Framework web
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Utilitaires de base
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Validation des données
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Base de données (optionnel pour le test)
# sqlalchemy>=2.0.0
# psycopg2-binary>=2.9.0

# Sécurité (optionnel pour le test)
# python-jose[cryptography]>=3.3.0
# passlib[bcrypt]>=1.7.0

# IA et Computer Vision (optionnel pour le test)
# opencv-python-headless>=4.8.0
# numpy>=1.24.0
# pillow>=10.0.0

# Tests
pytest>=7.0.0
pytest-asyncio>=0.21.0
