# Dépendances optimisées pour Windows - Backend ProctoFlex AI
# Seulement les packages essentiels qui fonctionnent sans problèmes

# Framework web principal
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Utilitaires de base
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Validation des données (version compatible)
pydantic-settings>=2.0.0

# Base de données SQLite (plus simple que PostgreSQL)
sqlalchemy>=2.0.0

# Sécurité
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0

# IA et Computer Vision (versions compatibles Windows)
opencv-python-headless>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# Tests
pytest>=7.0.0
pytest-asyncio>=0.21.0

# WebSockets pour communication temps réel
websockets>=11.0.0
